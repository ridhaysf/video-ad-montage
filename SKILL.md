---
name: video-ad-editor
description: يحوّل فيديو حديث-للكاميرا (سيلفي/تيليبرومبتر) إلى إعلان عمودي 9:16 كامل — يشيل السكتات، يفرّغ الكلام بتوقيت كل كلمة، يركّب كابشن عربي متزامن، ويبني موشن قرافيكس ومشاهد بي-رول مرسومة بالكود، وكرت نهاية بدعوة للتفاعل — كله بألوان صانع المحتوى نفسه. وفيه وضع ثانٍ للمونتاج: مجموعة مقاطع بلا كلام (مقاهي، سفر، منتج، مكان) يختار أحلى لحظة بكل مقطع ويركّبها مونتاجاً واحداً بإيقاع. ووضع ثالث للبودكاست: كاميرتان أو أكثر لنفس الجلسة (ضيف ومقدّم، أو شخص واحد بزاويتين) يزامنها بالصوت، يعرف من يتكلم، ويبني ريلاً بأوضاع: المتكلم ملء الشاشة، الرادّ ملء الشاشة، الاثنين فوق وتحت، وكرت صغير راسه طالع منه والكلام كولاج فوقه. ALWAYS use this skill when the user says "منتج هذا المقطع"، "سو من هذا الفيديو إعلان"، "شيّل السكتات"، "حوّل الفيديو لريل"، "ركّب كابشن عربي"، "ابي موشن قرافيكس على الفيديو"، "اقص الصمت"، "عندي ٣٠ مقطع سوّ لي منها فيديو"، "ركّب لي مونتاج من هالمقاطع"، "اختار أحلى اللقطات"، "عندي كاميرتين لنفس الجلسة"، "بودكاست"، "سوّ ريل من البودكاست"، "زامن الكاميرات"، أو يرسل فيديو كلام أو مجلد مقاطع أو ملفات كاميرات متعددة ويطلب مونتاجاً أو إعلاناً أو ريلاً. مو للكاروسيل (استخدم carousel-creator) ولا لكفر الفيديو (استخدم animated-video-cover).
---

# مونتاج إعلان الفيديو — بلا برنامج مونتاج

## الفكرة
المونتاج كله كود: ffmpeg يقص، وِسبر يفرّغ بتوقيت الكلمة، ومحرّك رسم يركّب الكابشن والموشن قرافيكس فوق الفيديو. المخرَج ملف MP4 واحد جاهز للنشر.

## ثلاثة أوضاع — حدّد وين انت بأول سطر

| | **إعلان كلام** (الافتراضي) | **مونتاج مقاطع** | **بودكاست** |
|---|---|---|---|
| المدخل | فيديو واحد فيه شخص يتكلم | مجلد فيه مقاطع كثيرة بلا كلام | كاميرتان أو أكثر لنفس الجلسة |
| مثاله | سيلفي · تيليبرومبتر · شرح | مقهى · سفرة · منتج · مكان | حوار بكاميرتين · شخص واحد بزاويتين |
| الاختيار على | الكلام (تشيل السكتات والجُمل المعادة) | المشهد نفسه (وضوح · حركة · إضاءة) | من يتكلم + الزاوية |
| فيه كابشن؟ | نعم، متزامن بالكلمة | ⛔ لا — ولا تفريغ أصلاً | نعم، كلمتين-ثلاث بكل مرة |
| الخطوات | 0-11 تحت | `references/montage.md` | قسم «وضع البودكاست» تحت |

**شلون تعرف الوضع بلا ما تسأله؟** مجلد مقاطع بلا كلام = مونتاج. ملف واحد فيه كلام = إعلان كلام. **ملفان أو أكثر لنفس الجلسة (نفس الكلام بزوايا مختلفة) = بودكاست.**
⛔ **لا تسأله «أي وضع تبي؟»** — اقرأ المدخل وامشِ، وقل له بجملة وحدة شنو فهمت.

## محرّكان — بس ⛔ لا تسأله أي سؤال عنهم

السكل واحد، والمحرّك اللي يرسم المشاهد اثنان. **القرار قرارك أنت، مو قراره.**

**ابدأ بالخفيف دائماً وبلا كلام.** لا تذكر كلمة «ريموشن» ولا «كانفس» ولا «محرّك» ولا تعرض عليه خيارين — الشخص اللي قدامك يبي إعلاناً، ما يبي يختار تقنية.

**متى تفتح الثاني؟** بس إذا **هو** قال شيئاً من هالنوع، بعد ما يشوف الإعلان:
> «أبي أعدّل بنفسي» · «ما عجبني مكان هالشي، أبي أحرّكه» · «فيه شاشة أشوف فيها التعديل؟» · «أبي أجرّب أشياء بنفسي»

عندها قل له جملة وحدة بلا مصطلحات: «أفتح لك شاشة تعديل مباشرة تشوف فيها الفيديو وتحرّك أي شي وتشوف النتيجة فوراً — تنزيلها ياخذ ٥ دقايق مرة وحدة، أبدأ؟»
وبعد موافقته: `04b_remotion.sh <work> setup` ثم `studio`.
**ما تعيد أي خطوة سابقة** — القص والتفريغ والكابشن والمؤثرات كلها مشتركة، ينتقل الشغل كما هو.

| | **الخفيف** (الافتراضي) | **شاشة التعديل** |
|---|---|---|
| متى | كل مرة، بلا سؤال | بس إذا طلب يعدّل بنفسه |
| التنزيل | صفر إضافي | ~500 ميقا مرة وحدة |
| ما يشوفه | لقطات تعرضها عليه | فيديو حي يسحب فيه ويشوف فوراً |
| الترخيص | حر | شركة بأربعة موظفين فأكثر تدفع (بلّغه إذا كان شركة) |
| الأمر | `04_render_frames.js` | `04b_remotion.sh` |

---

## ⛔ أسلوبك مع المستخدم — اقرأها قبل أي شي

**أنت اللي تجهّز، مو هو.** الشخص اللي قدامك يمكن ما يعرف شنو هو ffmpeg ولا يبي يعرف. ممنوع تعطيه قائمة أوامر ينفّذها.

- **لا تعرض عليه متطلبات ولا تسأله «عندك جذيه؟»** — افحص بنفسك، وإذا ناقص شي قل: «ناقصني أداتين أجهّزهم لك الحين، ياخذ دقيقتين — أبدأ؟» وبعد موافقته نزّلها بنفسك.
- **بلا مصطلحات.** لا تقول «puppeteer» ولا «كوديك» — قل «أداة ترسم الشرائح» و«جودة الفيديو».
- **قل له وين احنا كل خطوة**، وقدّر الوقت: «شلت السكتات — نصف الفيديو راح. الحين أفرّغ كلامك، ٣ دقايق.»
- **اطلب شي واحد بكل مرة.** لا تطلب الفيديو والألوان والحساب بجملة وحدة.
- **لا تسلّم بلا ما تريه.** بعد كل مرحلة مهمة، اعرض عليه لقطة أو ملخصاً.

---

## ⛔ وين يشتغل هذا السكل؟ على جهازك فقط
يشتغل عبر **Claude Code على جهازك** (ماك، ومن v2.8 ويندوز أيضاً بمساهمة @thefhiter؛ أنماط القصّ الثلاثة ماك فقط): يحتاج ffmpeg وموديل وِسبر (ينزل من openaipublic.azureedge.net) ومتصفحاً للرسم ومكتبة Vision للقصّ. **داخل تطبيق كلود بالسحابة (الساندبوكس) ما يشتغل**: تنزيل الموديل محجوب وما فيه متصفح. لو شغّله أحد من التطبيق وطلعت له رسالة عن host محجوب، الجواب: «افتح Claude Code على جهازك واختر Local وشغّل نفس الأمر». `00_setup.sh` يكتشف هذا ويوقف برسالة واضحة.

> ### ⚠️ ويندوز: اقرأ هذا قبل أي أمر
> الأوامر تحت مكتوبة بـ`python3` (ماك/لينكس).
> **على ويندوز `python3` اختصار متجر مايكروسوفت ويفشل — استخدم `python`.**
> `00_setup.sh` يطبع لك المفسّر الصحيح بأول سطر. سكربتات الـshell تكتشفه بنفسها.
> تفاصيل ويندوز كاملة بآخر الملف.

## الخطوة 0 — جهّز بصمت

```bash
bash scripts/00_setup.sh
```
- رجّع «كل شي جاهز» → لا تذكر الموضوع أصلاً، انتقل للخطوة التالية.
- طبع سطر «🆕 فيه إصدار أحدث» → قل للمستخدم بجملة وحدة: «فيه تحديث للسكل (v…) — أحدّثه لك الحين؟» وبعد موافقته شغّل `bash scripts/00_setup.sh --update` وكمّل بلا إعادة أي خطوة. (v3.0: التحديث بلا تنزيل يدوي)
- رجّع ناقصاً → قل له بجملة وحدة شنو راح تنزّل وليش، خذ موافقته، ثم:
```bash
bash scripts/00_setup.sh --install
```
لو فشل شي (مثلاً كروم مو منصّب) قل له الحل بجملة بشرية، بلا لصق رسالة الخطأ.

جهّز مجلد شغل، وانسخ له `scripts/compose.REFERENCE.html` باسم `compose.html` و`scripts/studio.html`.

---

## الخطوة 1 — هويته البصرية (ما فيه لون افتراضي)

⛔ **ممنوع تفترض الكريمي والطيني.** ذاك ثيم كلود، مو ثيم كل الناس.
⛔ **ولا تغيّر ثيم فيديو سابق ولا تبدّل الألوان الأساسية من عندك** — لو عند المستخدم `theme.json` من فيديو قبل، استخدمه كما هو. تغيير أي لون أساسي يكون بطلب صريح منه فقط (قاعدة المستخدم 13 سبتمبر).

1. دوّر `.auto-memory/creator-profile.md` — إن وُجد خذ منه الألوان والخط والحساب، وقل له: «راح أمشي بألوانك: كذا وكذا — تمام؟»
2. ما فيه ملف؟ اسأل **سؤالاً واحداً**: «شنو ألوان حسابك؟ عطني لون الخلفية ولون التمييز وشعارك (أو رابط حسابك وأنا أطلّعها).»
3. اكتب `<work>/theme.json`:
```json
{ "bg":"#101828", "ink":"#F5F7FA", "acc":"#F2B33D", "clay":"#C98B18", "mut":"#98A2B3",
  "font":"Tajawal", "handle":"@his_handle", "logo":"logo.png", "grade": false, "badge": false,
  "faceAnchor": 0.30, "badgeUntil": 0 }
```
**شارة الحساب مطفية افتراضياً** (`badgeUntil: 0`) — غير مرغوبة، والاسم يبقى بكرت النهاية فقط. `badgeUntil: 3` تعيدها أول ٣ ثوانٍ، و`badge:false` مدعومة للتوافق. كل الرسم يشتق ألوانه من هذي القيم أوتوماتيكياً — الكروت والظلال ولون النص فوق أزرار التمييز (يُحسب من إضاءة اللون). خلفية غامقة أو فاتحة، الاثنتان تشتغلان.

**⛔ صفر تعديل لوني افتراضياً (قرار المستخدم 8 سبتمبر):** الفيديو يطلع بألوانه كما صوّره، لا تدرّج ولا تشبّع ولا بلر. وانتبه لفخ HDR: فيديو الآيفون يجي HDR (HLG/Dolby Vision)، وتحويله الساذج لـSDR يطلع باهتاً وبارداً فيحسبه المستخدم «تعديلاً لونياً». `03_cut_zoom.py` صار يكشفه ويحوّله بمحوّل أبل (`hdr2sdr.swift`، يُبنى تلقائياً بأدوات Xcode) فيبقى بنفس مظهره على الجوال.

**التدرّج اللوني (`grade`) مطفي افتراضياً** — الفيديو يطلع بألوانه الأصلية. لا تشغّله إلا إذا طلبه صراحة أو اشتكى إن الصورة باردة/باهتة. وإذا شغّلته قل له إنك سويت ذلك.

انسخ شعاره في مجلد الشغل باسم `logo.png`.

---

## الخطوة 2 — خذ منه الفيديو

اطلبه بجملة بسيطة، واقبل أي طريقة:

| الطريقة | متى | شلون |
|---|---|---|
| **ملف على جهازه** ← الأفضل | دائماً إن أمكن | يعطيك المسار، تنسخه `<work>/src.mov` — بلا انتظار تحميل |
| **رابط قوقل درايف** | يصوّر بالجوال والدرايف يرفع تلقائياً | خلّه يضبط المشاركة «أي شخص لديه الرابط»، ثم:<br>`curl -sL "https://drive.usercontent.google.com/download?id=<ID>&export=download&confirm=t" -o <work>/src.mov`<br>الـID هو الجزء بين `/d/` و`/view` بالرابط. مجرَّب على ملف 441 ميقا |
| **كونكتور قوقل درايف** | الملف خاص وما يبي يغيّر المشاركة | استخدم أدوات الكونكتور المتاحة بالجلسة |

**عن الدقة:** 4K أفضل لأن الزوم يُقص من الأصل فما تخسر وضوح. بس 1080p يشتغل عادي — الفرق إن مجال الزوم يصير أضيق. **لا ترفض فيديو 1080p ولا تطلب منه يعيد التصوير.**

تحقق أنه وصل: `ffprobe -v error -show_entries format=duration -of csv=p=0 <work>/src.mov`

---

## الخطوات 3-11 — الإنتاج

### 3) خطة القص
```bash
python3 scripts/01_cut_plan.py <work>
```
قل له كم انشال: «شلت 52 ثانية سكوت — الفيديو صار 46 بدل 98.»

### 3.5) «حسّن الصوت» (اختياري، قبل التفريغ)
لو التسجيل بمايك الجوال بصدى أو ضجيج، أو قال «حسّن الصوت» → اقرأ `references/audio-enhance.md` (أدوبي أو محلي بـ`02b_enhance_audio.sh`).

### 4) التفريغ بتوقيت الكلمة
```bash
ffmpeg -v error -i <work>/src.mov -vn -ac 1 -ar 16000 -y <work>/a.wav
python3 -m whisper <work>/a.wav --language ar --model medium --word_timestamps True \
  --output_format json --output_dir <work> --fp16 False
```
ياخذ دقايق — شغّله بالخلفية وقل له كم يتوقع.

### 5) التصحيح والكابشن
اقرأ `a.json`، صحّح كل جملة (وِسبر يغلط بالعامية الخليجية)، واكتب `<work>/fixes.json`:
```json
{ "fix": [["كل","شي","تشوفه"], ["الكابشن","الزوم"]], "hot": ["تشوفه","الزوم"] }
```
`hot` = الكلمات اللي تنحبس بقرص التمييز وقت نطقها. **عدد كلمات كل جملة لازم يساوي عدد كلمات وِسبر لنفس الجملة** وإلا تختل التوقيتات (السكربت يوقفك لو اختلفت).
```bash
python3 scripts/02_captions.py <work>
```

### 5.5) اعرض عليه النص — وشيل أي جملة ما عجبته ← ميزة قوية، لا تتخطّاها

⛔ **إلزامي بكل فيديو:** بعد التصحيح **اطبع السكربت كاملاً بالشات** (مرقّماً بالتوقيت) قبل ما تصمّم أي مشهد — المستخدم يبي يشوف كلامه مكتوباً قدامه ليقرر شنو يقص أو يعدّل. لا تكتفِ بكتابة `script.txt` على القرص (بلاغ المستخدم 13 سبتمبر).
```bash
python3 scripts/10_script_edit.py <work> show
```
يطبع كلامه مرقّماً بالتوقيت ويكتب `script.txt`. **اعرض عليه القائمة بالشات وقل له: «شنو تبي أشيل؟»**

**🔁 والأهم — الجملة المعادة:** السكربت ينبّهك تلقائياً لأي جملتين متشابهتين خلال جملتين.
لما يعيد المتحدث صياغة جملة، **الأولى هي الغلط والثانية هي التصحيح** — اعرض الزوج عليه واقترح حذف الأولى:
> «قلت الجملة مرتين — ‹قدرنا نحل السبب› ثم ‹قدرنا نعرف السبب›. أشيل الأولى؟»
```bash
python3 scripts/10_script_edit.py <work> dupes
```
```bash
python3 scripts/10_script_edit.py <work> drop 6 8       # يشيل الجملتين من الفيديو والصوت
python3 scripts/10_script_edit.py <work> keep 1 2 5 9   # يبقي هذي بس (لمقطع مختصر)
python3 scripts/10_script_edit.py <work> undo           # تراجع
```
الجملة تنشال من الفيديو والصوت، وكل اللي بعدها ينزاح، و`cut.json` و`caps.json` و`sfx.json` تتحدّث كلها.

**⚠️ سوّها هني — قبل تصميم المشاهد.** لو شيّلت جملة بعد ما صمّمت المشاهد، أوقاتها كلها تنزاح ولازم تعدّلها.
وبعد أي حذف: أعد `03_cut_zoom.py` واستخراج الفريمات، والرسم بـ`--force`.

### 6) القص والزوم
```bash
python3 scripts/03_cut_zoom.py <work>
mkdir -p <work>/vfr && ffmpeg -v error -i <work>/cutz.mp4 -vf fps=30 -q:v 3 -y <work>/vfr/%05d.jpg
```

### 7) صمّم المشاهد ← أهم خطوة
انسخ `compose.REFERENCE.html` إلى `<work>/compose.html` وأعد كتابة دوال المشاهد.

**البنية جاهزة، لا تلمسها:** تصغير الفيديو داخل كرت (`R_FULL` / `R_DOWN` / `R_LOWER` بانتقال ناعم)، شارة الحساب، شريط التقدّم، كروت الكابشن مع تظليل الكلمة المنطوقة، كرت النهاية، واشتقاق الألوان من الثيم.

**اللي تبتكره:** المشاهد. اعصف ٣-٤ أفكار لكل جملة، والفكرة لازم تكون **استعارة بصرية لما يقوله**، لا زينة:

| يقول | المشهد |
|---|---|
| يعدّد أشياء | بطاقات تدخل وحدة مع كل كلمة ثم تنقلب بعلامة صح |
| رقم أو سعر | عدّاد يلف وينزل على الرقم بضربة |
| «فرّغ كلامي» | لوحة تفريغ، كل كلمة تنزل بسطرها وتوقيتها |
| «انكسر / خطأ» | قليتش يزحزح شرائح الصورة + شقوق |
| مشكلة تقنية | كرت الفيديو يسوَدّ وفوقه علامة تحذير |
| إصلاح | شريط تقدّم + قائمة تتعلّم بصح |
| «ملف واحد» | كرت ملف وبطاقات تطير وتندمج فيه |
| دعوة للتعليق | مربع تعليق والكلمة تنكتب حرف حرف |

كل دالة مشهد تاخذ `t` وترسم حسب توقيت الكلمة من `caps.json` — المشهد يلتصق بالكلمة، لا بتوقيت تقريبي.

**⛔ ما فيه شارة حساب فوق الفيديو** (`badgeUntil: 0` — الافتراضي): اسمه مكتوب بالمنصة نفسها وبكرت النهاية، وأعلى الشاشة مساحة للرسم. لو طلبها أحد: `badgeUntil: 3` تخليها أول ٣ ثوانٍ بس.

**⛔ قاعدة التخطيط (معتمدة من المستخدم — لا تخالفها):**

| اللحظة | المستطيل | الشكل |
|---|---|---|
| كلام بلا رسم | `R_FULL` | وجهه ملء الشاشة، الكابشن تحت عند 1460 |
| أي رسم أو موشن | `R_DOWN` (الافتراضي) | **الرسم فوق (y 280-520) ← الكابشن راكب على حافة الفيديو ← وجهه تحت بعرض الشاشة كامل** |
| بي-رول أو لوحة كبيرة | `R_LOWER` | الكرت الكبير فوق ← الكابشن ← وجهه صغيراً تحت |

**ليش:** الوضع القديم (`R_STAGE`/`R_SIDE`: الفيديو بالنص، الرسم فوق راسه، الكابشن تحت) يخلق ثلاث نقاط تركيز متباعدة والمشاهد يتوه.

**ثلاث تفاصيل تفرّق (٢ سبتمبر):**
1. **الفيديو بعرض الشاشة كامل بلا هوامش ولا زوايا مدوّرة** — الوجه يطلع أكبر بمرة ونصف من الكرت الضيق. الثمن: أطراف الكادر فوق وتحت تنقص، وهذا مقبول لأن الوجه هو المهم.
2. **الكابشن يركب على حافة الفيديو** (‎42٪ من ارتفاعه فوق الحافة و‎58٪ تحتها) — يربط نصفَي الشاشة فما تبين قطعتين ملزوقتين.
3. **شبكة خفيفة بالخلفية** كل 60px بشفافية 7.5٪ — تعطي عمقاً بلا ما تسحب النظر (`grid:false` بـtheme.json توقفها).

**⛔ لا تفرط بـ`R_DOWN`.** هو الافتراضي للحظات الرسم، مو لكل الفيديو:
- **الهوك (أول ٣-٤ ثوانٍ) ملء الشاشة دائماً** — وجهه كامل، بلا لوحة تسحب النظر.
- كل جملة فيها **ذروة أو انفعال أو سؤال للمشاهد** → ملء الشاشة، خله يشوف عينه.
- **لا تتجاوز نصف مدة الفيديو** بـ`R_DOWN`، ولا تخلّه متواصلاً أكثر من **٨ ثوانٍ** بلا لقطة ملء شاشة بينهما — وإلا صار الفيديو لوحة ثابتة ووجه صغير تحتها.
- ما فيه رسم بهاللحظة؟ إذاً ملء الشاشة. **اللوحة تجي للفكرة، مو للتعبئة.**

⚠️ **«ملء الشاشة» يُعرف بالمساحة لا بالزوايا** (`isFull()`): R_DOWN صار بلا زوايا مدوّرة مثل R_FULL، فأي فحص قديم يعتمد على `r` ينخدع ويشغّل «الكلام ورا الشخص» بلحظة مو ملء الشاشة.
بـ`R_DOWN` العين تمشي بخط واحد من فوق لتحت. **والكرت مرن:** يصغر نسبة وتناسب حسب أسفل الرسم (`gb` بكل مشهد، مثال `{s,e,m:R_DOWN,gb:480}`) وعدد أسطر الكابشن بتلك اللحظة، فالرسم والكابشن ما يتزاحمان أبداً. اللوحات تُرسم بإحداثيات `130..950 × 278..458` داخل `panelIn()` وهي تتكبّر 1.2 وحدها.
`R_STAGE` و`R_SIDE` باقيتان بالملف للتوافق فقط — **لا تستخدمهما بفيديو جديد.**

**لقطات B-roll (اختياري):** لو عطاك فيديو ثانياً (لقطات جرافيكس من الشركة مثلاً)، استخرج المقاطع المفيدة فريمات إلى `<work>/broll/<اسم>_%04d.jpg`،
عرّف مداها في `BR_NEED`، واعرضها بـ`brCard()` مع `R_LOWER`. `BRCROP` يقصّ الترجمة المحروقة أسفل اللقطة. ٣-٤ لقطات بالفيديو كافية.

**وضع «الشرح فوق والفيديو تحت» (`R_LOWER`)** — للبي-رول واللوحات الكبيرة:
الفيديو ينزل تحت ويملأ أسفل الشاشة (وراس المتحدث ينقص من فوق شوي، وهذا مقصود ومقبول بصرياً)،
واللوحات والكابشن كلها تطلع فوقه. **هذا الوضع يدخل حزام انستقرام عن قصد** — ومسموح، لأن المغطّى صورة لا نص.
استخدمه لما تكون اللوحة كبيرة (قوائم، مقارنات، جداول) وما يكفيها النصف العلوي.
الكابشن ينتقل فوق كرت الفيديو تلقائياً بهذا الوضع.

**⛔ المنطقة الآمنة — انستقرام يغطي أطراف الشاشة بأزراره:**

| المنطقة | لا تحط فيها نصاً |
|---|---|
| أعلى | أول 150 بكسل |
| أسفل | آخر 300 بكسل (وحزام حذر من 1500) |
| يمين | 180 بكسل بعرض y من 1100 إلى 1750 (لايك · تعليق · مشاركة) |

الملف المرجعي مضبوط أصلاً: الشارة عند 190، شريط التقدّم عند 1600، وحافة كرت الكابشن السفلى 1500.
**وأول كابشن لازم يبان بأول نصف ثانية** — الهوك المتأخر يخسر نصف المشاهدين قبل ما يبدأ الكلام.

عاين قبل ما ترسم كل شي:
```bash
node scripts/04_render_frames.js <work> preview 4.6 12.3 27.6 31.0 48.4
bash scripts/07_contact_sheet.sh <work> <work>/sheet.jpg 4.6 12.3 27.6 31.0 48.4
```

**فتحت له شاشة التعديل (بطلبه)؟** المشاهد تنكتب بـ`<work>/remotion/src/Scenes.tsx` (نفس المنطق: كل مشهد ياخذ `t`):
```bash
bash scripts/04b_remotion.sh <work> setup            # مرة وحدة — بعد إذنه (~500 ميقا)
bash scripts/04b_remotion.sh <work> studio           # تايم-لاين حي على المتصفح
```
مستطيلات عرض الفيديو تنكتب بـ`<work>/stage.json` ونصوص الختام بـ`<work>/outro.json` — وتنعكس بالمحرّكين.
**اقرأ `sheet.jpg` وحدها — لا تقرأ الصور وحدة وحدة.** ورقة واحدة = قراءة واحدة بدل خمس. **لا ترسم الفيديو كامل قبل ما تعاين ٦ لقطات على الأقل**، واعرض الورقة على المستخدم.

```bash
python3 scripts/18_studio.py <work>      # يطبع رابطاً تفتحه بجوالك على نفس الواي فاي
```
استوديو تفاعلي (سحب على التايم-لاين ورسم مباشر):
```bash
python3 -m http.server 8791 --directory <work>   # ثم /studio.html
```

### 7.4) تسجيل الشاشة (v2.9 — قواعد المستخدم 13 سبتمبر)
لما يعطيك تسجيل شاشة من الجوال (خطوات داخل تطبيق) مع فيديو الكلام، اقرأ `references/screen-recording.md` — فيه القواعد الخمس وشيفرة `fullShot()/underline()` الجاهزة. الخلاصة:
1. **التسجيل بنسبته الأصلية دائماً** — لا قصّ ولا ضغط بالعرض أو الطول؛ تصغير أو تكبير نسبي فقط (`Math.min` contain)، وإلا الدوائر تطلع بيضاوية.
2. **داخل إطار جوال مرسوم بالكود** بنفس نسبة التسجيل (زوايا مدورة + جزيرة ديناميكية)، ضمن المنطقة الآمنة (y 150–1480)، والخلفية بالشبكة تبقى وراه. المتحدث يختفي بهاللحظة (`R_OFF`) والكابشن كذلك.
3. **كت مباشر بين اللقطات** — بلا دخول/خروج ولا زوم؛ كل لقطة 1.5–3 ثوانٍ عند الخطوة اللي يقولها.
4. **علامة الضغط** دائرة نابضة بلون التمييز على النقطة بالضبط (إحداثيات بكسل المصدر، مو نسب) — واستخدم `underline()` لخط تحت كلمة بقائمة.
5. **شريط التقدّم ينزل للحزام السفلي** أثناء تسجيل الشاشة ويرجع بعده (اسمح له بـ`safe.json` — خط بلا نص).

### 7.5) أنماط القصّ الثلاثة (اختيارية — ماك)
«الكلام ورا الشخص» (الكشيدة تمرّ ورا راسه) · «واقف قدام اللوحة» · «راسك برّا المستطيل». مرة أو مرتين بالفيديو، بالهوك أو الذروة.
الأوامر والقواعد المعتمدة (8 سبتمبر: `R_LOWER` + headout + `nocap`) في `references/cut-styles.md` — اقرأه قبل ما تستخدم أي نمط.

### 7.6) 🔤 حركات الكلمة العربية (v3.1) — الحروف تبقى متصلة

حركة على أي كلمة لحظة نطقها، بلا ما تنفك حروفها. تُحدَّد بـ`fixes.json` قبل `02_captions.py`:
```json
{ "fix": [...], "hot": [...], "fx": { "خلاص": "drop", "متصلة": "type", "تكسّرها": "crack", "تتحرّك": "slam", "وتتمدد": "stretch" } }
```
| الحركة | الاسم | شكلها | بسكربت الريتم |
|---|---|---|---|
| طيحة | `drop` | تطيح من فوق بجاذبية وترتد شوي، والكادر يندفع لتحت لحظة الهبوط | `[طيحة: «خلاص»]` |
| اهتزاز | `shake` | تهتز والكادر يرجف معها بخفة | `[اهتزاز: «تهتز»]` |
| انكسار | `crack` | اهتزاز + شقوق بيضاء على الشاشة | `[انكسار: «تكسّرها»]` |
| كتابة | `type` | تنكشف من اليمين كأنها تنكتب — حرف حرف ومتصلة | `[اكتب: «متصلة»]` |
| نبضة | `pulse` | تكبر وترجع | `[نبض: «خلاص»]` |
| تمدد | `stretch` | نسخة ذهبية تتمدد بالكشيدة فوق الكابشن | `[تمدد: «تتمدد»]` |
| ضربة | `slam` | تهبط ضخمة من النص وتضرب ثم تصغر لمكانها | `[ضربة: «مجانية»]` |

**⛔ قواعد الكشيدة (لا تُكسر):** الكشيدة تنحط بعد حرف يتصل بما بعده فقط — الحروف `ا أ إ آ د ذ ر ز و ؤ ة ء ى` ما تمتد بعدها. كلمة بلا وصلة صالحة = بلا تمدد (تنبض بدلها). الأرقام ما تتمد. التمدد بالبكسل (شريحة من وسط الكشيدة) لا بعدد الحروف حتى ما يرجف.

**ستايلات الكابشن** (`theme.json` ← `capStyle`): `card` (كرت بلون الهوية، الافتراضي) · `shadow` (كبير أبيض بظل بلا كرت) · `big` (كلمة وحدة ضخمة) · `underline` (خط تحت الكلمة المنطوقة) · `bar` (شريط بلون التمييز خلفها) · `type` (ماكينة كاتبة بمؤشر). بكل الستايلات: الرقم ووحدته بسطر واحد، والجملة الطويلة يصغر خطها لتدخل بسطرين بدل تقليب صفحات.

**الكلام ورا الراس** (`11_behind_text.js build <أرقام الجمل 1-based>`): صار بالبكسل وبقواعد صارمة — الحروف دايماً برّا الراس (الكشيدة وحدها تغطيه)، الكلمة كلها داخل الكادر وإلا يصغر الخط، والكلمة اللي بلا موضع كشيدة (مثل «ورا») تنزل كرتاً على الصدر. التفاصيل بـ`references/cut-styles.md`.

### 7.7) مشهد «شرح كامل» وإيقاع «هادي» (v2.5)

**مشهد «شرح كامل» (`R_OFF`)** — الوضع الرابع بعد ملء الشاشة والكرت المرن والكرت الصغير:
المتحدث **يختفي** (كرته ينزلق تحت الشاشة)، والشاشة كلها إنفوقرافيك، **صوته يكمّل والكابشن يبقى تحت** بمكانه المعتاد.
بالمشاهد: `{s:12.4,e:18.9,m:R_OFF}` — البنية تتكفّل بالانزلاق والكابشن والشريط.

| اللحظة | المستطيل | الشكل |
|---|---|---|
| كلام بلا رسم | `R_FULL` | وجهه ملء الشاشة |
| رسم أو موشن | `R_DOWN` | الرسم فوق ← الكابشن ← وجهه تحت بعرض الشاشة |
| بي-رول أو لوحة كبيرة | `R_LOWER` | اللوحة فوق ← الكابشن ← وجهه صغيراً تحت |
| **شرح كامل** | **`R_OFF`** | **بلا وجه — الشاشة كلها للشرح، صوته يكمّل** |

**⛔ قواعده:**
1. **مرة أو مرتين بالفيديو** — أكثر يحس المشاهد إنك اختفيت.
2. **4 إلى 8 ثوانٍ** لكل مشهد، وبعده ملء شاشة مباشرة (يرجع الوجه).
3. **عند الجمل اللي فيها أرقام أو قائمة أو مقارنة** — الشي اللي يحتاج مساحة فعلاً. **لا بالهوك ولا بالدعوة الأخيرة.**
4. المشهد لازم يكون **استعارة بصرية لكلامه** (عدّاد، بطاقات، جدول) — مو زينة تملأ الفراغ.
5. المحرّك الكانفسي فقط حالياً (Remotion ما يعرفه بعد).

**إيقاع «هادي» (`theme.json` ← `"pace":"calm"`)** — لما يقول المستخدم «بإيقاع هادي» أو يشتكي من كثرة القطعات:
- الزوم أخف (1.00-1.06 بدل 1.00-1.14) وأقل تنوّعاً.
- **ما يتغيّر الزوم قبل ما تمر 4 ثوانٍ** على نفس اللقطة (`03_cut_zoom.py` يجمّع المقاطع القصيرة على زوم واحد).
- وبالمشاهد: **لا تبدّل المستطيل قبل 4 ثوانٍ**، ولا تتجاوز **6 تغييرات بكل 30 ثانية**، والشرح يثبت لين تخلص الفكرة.
- الهوك يبقى ملء الشاشة زي ما هو.
بدون ما يطلبه، الإيقاع يبقى الحالي — ما يتغيّر شي على أحد.

### 7.75) خلفية باهتة (اختياري)
خلفية تصوير سيئة؟ `14_backdrop.py <work> 0.62` يغطّيها بلون الثيم والمتحدث يبقى واضحاً. التفاصيل: `references/backdrop.md`.

### 7.8) أسلوب «كولاج» (v2.6)
الشاشة ورقة، وهو قصاصة، وكل جملة تجيب عنصرها — بلا كابشن. أمر واحد: `node scripts/13_collage.js <work> collage all`.
**قبل ما تشغّله اقرأ `references/collage.md`** (10 قواعد معتمدة من المستخدم 4 سبتمبر).

### 8) المؤثرات الصوتية
```json
{ "outro": 5.2, "whoosh_up": [3.1,11.25], "whoosh_down": [7.85],
  "thud": [27.27,29.47], "tap": [23.08,24.06] }
```
```bash
python3 scripts/05_sfx.py <work>
```

### 9) الرسم النهائي والتجميع

**الخفيف:**
```bash
node scripts/04_render_frames.js <work> all          # يكمّل من وين وقف — ما يعيد فريماً جاهزاً
bash scripts/06_encode.sh <work> <work>/ad-final.mp4
```
عدّلت مشهداً واحداً بعد الرسم؟ لا تعيد الكل — أعد نافذته بس ثم جمّع:
```bash
node scripts/04_render_frames.js <work> range 26.4 31.2
```
(`--force` مع `all` يعيد الرسم من الصفر. السكربت ينبّهك لو بقي فريم ناقص قبل التجميع.)

**ريموشن:** يطلّع MP4 مباشرة بلا فريمات:
```bash
bash scripts/04b_remotion.sh <work> render <work>/ad-final.mp4
```

### 10) معايرة الصوت (+ خلفية صوتية اختيارية)
```bash
bash scripts/06b_master.sh <work> <work>/ad-final.mp4 <work>/ad-master.mp4
```
يرفع الصوت لـ‎-14 LUFS — نفس علو باقي الفيديوهات بالفيد؛ بدونه صوته يطلع أخفض من اللي قبله وبعده.
ولو حطّيت `<work>/bg-audio.mp3` ينضاف **ملف صوتي بالخلفية ينخفض تلقائياً كل ما يتكلم** ويرجع بالسكتات. الصورة تُنسخ كما هي بلا إعادة ترميز.

**التسمية:** قل «ملف صوتي بالخلفية» لا «موسيقى» — المحتوى يحدّده هو، وانت تتعامل مع الملف كما هو.

### 11) ملف الترجمة
```bash
python3 scripts/09_srt.py <work> ad-master
```
يطلّع `.srt` (يوتيوب ولينكدإن يقراه) و`.txt` = نص كلامه كامل جاهز لكابشن البوست.

---

# وضع المونتاج — مقاطع بلا كلام
مجلد مقاطع (مقهى · سفرة · منتج) → فيديو واحد بإيقاع، بلا تفريغ ولا كابشن ولا ثيم. المحرّك: `12_montage.py` (`scan` · `sheet` · `drop/keep` · `plan` · `build`) ثم `06b_master.sh` مع `bg-audio.mp3`.
**اقرأ `references/montage.md` أول ما يعطيك مجلد مقاطع** — فيه القواعد الست وطريقة الاختيار.

# وضع البودكاست — كاميرتان أو أكثر (v2.7)

كاميرتان (أو أكثر) لنفس الجلسة → ريل 9:16 واحد. ماك فقط (الوجوه وقصّ الشخص بمكتبة Vision). **أمران بس** — كل شي بينهما آلي:

```bash
python3 scripts/15_podcast.py prep <work> <مجلد الكاميرات | ملف1 ملف2 …>
#  يزامن الكاميرات بالصوت · يفحص العطب بكل كاميرا (نصف كادر أسود مثلاً) ويستبعده وحده
#  يرصد الوجوه والفم → من يتكلم متى · هل الكاميرتان لنفس الشخص؟ · يفرّغ الكلام ويطبعه مرقّماً
#  (يطلّع الفريمات بالتوازي — 3-5 دقايق لفيديو دقيقة)
#  ← صحّح النص بـfixes.json (نفس صيغة وضع الكلام: عدد الكلمات لازم يساوي، الكلمة المحذوفة = "")
python3 scripts/15_podcast.py make <work>
#  كابشن · خطة مشاهد · قصّ الراس · رسم · تجميع · معايرة · srt · ورقة لقطات sheet.jpg
```

**الأوضاع الأربعة (يخطّطها السكربت بنفسه، بإيقاع هادي):**

| الوضع | متى | الشكل |
|---|---|---|
| `FULL` | المتكلم يتكلم | كاميرته ملء الشاشة (وزوم خفيف 1.08 بالتكرار) |
| `FULL` للثاني | يردّ عليه | كاميرته ملء الشاشة |
| `SPLIT` | حوار سريع، أو كل ثالث تبديل | المتكلم فوق والثاني تحت بخط بلون التمييز |
| `HEAD` | جملة فيها رقم أو كلمة ساخنة (مرة أو مرتين) | كرت صغير تحت وراسه طالع فوق حافته، والكلام قصاصات كولاج فوق — بلا كابشن |

**نفس الشخص بزاويتين؟** (يكتشفه من تطابق حركة الفم) → ينوّع بالزاوية: القريبة (الوجه الأكبر) بالهوك، ثم قريبة/مقسّم/واسعة/زوم بالتناوب.
**قواعد الإيقاع:** الهوك ملء الشاشة بالقريبة · ما يبدّل قبل 4 ثوانٍ · ما يطوّل فوق 9 · مشهد `HEAD` 3-8 ثوانٍ وبينهما 8 على الأقل.

**الثيم:** `theme.json` بمجلد الشغل (أو بمجلد الكاميرات وينسخه) — نفس مفاتيح وضع الكلام + `capStyle: "shadow"` (نص أبيض بظل بلا خلفية، كلمتين-ثلاث، الكلمة المنطوقة بلون التمييز) أو `"card"` · `fontFiles` لخط محلي (`[{src, weight}]`) · `logo` · `site`. **الألوان من الشخص نفسه (الخطوة 1) — ما فيه ثيم افتراضي.** الأرقام غربية دائماً حتى لو الخط يرسمها هندية.

**بعد `make`:** اقرأ `sheet.jpg` وحدها واعرضها عليه. تعديل مشهد؟ عدّل `plan.json` ثم `node scripts/16_render_pod.js <work> range <من> <إلى>` وأعد التجميع من `make` (يكمّل بلا إعادة القصّ). تعديل نص؟ `fixes.json` ثم `make`.
**ما ينقص السكتات** (الحوار طبيعي فيه وقفات) — لو طلبها، `10_script_edit.py drop` يشيل جملاً كاملة.
**ملفات المخرج:** `pod-master.mp4` · `pod-master.srt` · `pod-master.txt` · `sheet.jpg`.

## ⛔ القواعد الثابتة

1. **المؤثر الصوتي يُربط بلحظة لها معنى** — رقم ينزل، شي ينكسر، مشهد ينتقل. المشكلة مو نوع الصوت، المشكلة رشّه على كل كلمة (جرّبناها وطلعت إزعاجاً). لا تتجاوز 15 حدثاً بالدقيقة، والذروة تحت ‎-18 dBFS، واعرض على المستخدم وين حطّيتها قبل التصدير.
2. **أرقام غربية دائماً** (0-9).
3. **الألوان من `theme.json` لا غير** — لا لون ثابت بالكود.
4. **ممنوع أي لون أو فلتر فوق صورته** — لا تدرّج ولا صبغة ولا LUT ولا طبقة ملوّنة على الفيديو. الصورة تطلع بألوانها الأصلية دائماً، إلا إذا طلب هو صراحة. (الألوان للكروت والنصوص فقط.)
5. **صحّح تفريغ وِسبر** قبل الكابشن.
6. **لا تخترع محتوى ما قاله المتحدث.** كل نص من كلامه؛ ولو احتجت وصف شي ما تعرفه، عمّم بدل ما تخمّن.
7. **لا نشر ولا جدولة** — التسليم ملف بس.
8. **«ورا الشخص» مرة أو مرتين بالفيديو** — لا أكثر، وإلا فقد أثره.
9. **سمّها «ملف صوتي بالخلفية»** — مو «موسيقى». المستخدم يحدّد محتواه (صوت بشري، أجواء، أو أي شي)، وانت تسمّيه بصيغته المحايدة وتحطه بـ`bg-audio.mp3`.
10. **اخترع مشاهد جديدة كل مرة.** الملف المرجعي مكتبة أنماط، مو قالباً يُنسخ.
11. **أي صورة أو تسجيل شاشة أو شعار يُعرض بنسبته الأصلية** — تصغير/تكبير نسبي فقط، ممنوع الضغط أو القصّ إلا بطلب صريح (المستخدم 13 سبتمبر).
12. **مصدر آيفون HDR → محوّل أبل إلزامياً** (`03_cut_zoom.py` يكشفه) — التحويل الساذج يبهّت الألوان ويرفع الظلال (لمسة كريمية)؛ لو شكّيت قِس متوسط لون البشرة بين المصدر والناتج.

---

## ✅ التحقق قبل التسليم (إلزامي)

0. **المنطقة الآمنة والهوك** — فحص آلي، ما يحتاج عينك:
```bash
node scripts/08_safe_check.js <work> --shot
```
يرسم كل لحظة مرتين بلونين مكان الفيديو، واللي ما يتغيّر = رسمك أنت — فيعدّ نصوصك داخل مناطق أزرار انستقرام بدقة، ويتأكد إن أول كابشن قبل نصف ثانية. يخرج بالرمز 3 لو فيه خرق، ويطلّع `safe.jpg` واللقطة الحمراء توريك وين المشكلة.
الحدود تُعدّل بملف `<work>/safe.json` لو احتجت (مثلاً فيديو للتيك توك بحدود ثانية).

1. **المزامنة** — فرّغ صوت الناتج مرة ثانية وقارن بدايات الجُمل بـ`caps.json`؛ الفرق أقل من 0.1 ثانية:
```bash
ffmpeg -v error -i <work>/ad-final.mp4 -vn -ac 1 -ar 16000 -y <work>/fa.wav
python3 -m whisper <work>/fa.wav --language ar --model small --output_format json --output_dir <work> --fp16 False
```
2. **الصوت** — بعد `06b_master.sh` يطبع العلو النهائي: لازم ≈ ‎-14 LUFS والذروة ‎-1.5 dBTP أو أقل.
3. **العين** — ورقة تواصل بـ٦ لقطات، تُشاف فعلاً.
4. **الحجم** — تحت 30 ميقا.

## 💰 توفير التوكنز — بلا مساس بالجودة

**بالقياس الفعلي (10 سبتمبر):** فيديو 36 ثانية كلّف 3.1 مليون توكن موزون — 41٪ أوامر طرفية متفرقة (كل أمر يعيد المحادثة كاملة)، 8 صور معاينة أكلت نحو 900 ألف حرف، والباقي ردود. الهدف: نفس المخرج بثلث الكلفة.

1. **أمر واحد لكل مرحلة، مو أمر لكل خطوة.** البودكاست أمران (`prep` ثم `make`). وضع الكلام: اجمع الخطوات المستقلة (استخراج الصوت + التفريغ + الفريمات) بأمر واحد يشتغل بالخلفية، وانتظر إشعاره مرة وحدة.
2. **ورقتان بالكثير لكل فيديو:** وحدة بعد التصميم، ووحدة بعد التصحيح. كل لقطة تُصغَّر (`scale=300:-1`) وتنجمع بورقة (`07_contact_sheet.sh`). أي فحص ثالث يكون بالأرقام (`08_safe_check.js` · المزامنة · LUFS)، لا بالعين.
3. **لا تقرأ ملفاً كاملاً:** `grep -n` للدالة، و`tail -2` لمخرجات ffmpeg، والسجلات الطويلة تُقرأ آخر 5 أسطر.
4. **المراجع عند الحاجة فقط:** أقسام الكولاج والمونتاج وأنماط القصّ وويندوز بـ`references/` — لا تفتحها إلا لما يوصلك المدخل لها.
5. **ملف إعدادات للعميل:** بعد أول فيديو، `theme.json` (الألوان والخط والشعار وأسلوب الكابشن) يُحفظ عنده، والفيديو الثاني يبدأ منه بلا تجربة وخطأ.
6. **جلسة نظيفة لكل فيديو**، وردود قصيرة: سطران وصورة. كلامك أنت أغلى شي (خمسة أضعاف المقروء).
7. **احفظ `caps.json` و`cut.json`** — أي تعديل لاحق ما يحتاج إعادة تفريغ.

⛔ **لا تعرض صورة إلا لسؤال بصري ما ينجاوب بغيرها.**

## التسليم
اعرض الملف مع: المدة، كم انشال من السكتات (أو بالبودكاست: عدد الكاميرات وفرق المزامنة وأي عطب استُبعد)، عدد المشاهد، والعلو النهائي. وسلّم معه `.srt` و`.txt` (نص كلامه لكابشن البوست). واذكر أنك ما نشرت شيئاً.

---

## خريطة السكربتات

| | يسوي شنو | المحرّك |
|---|---|---|
| `00_setup.sh` | يفحص الأدوات وينزّل الناقص | مشترك |
| `01_cut_plan.py` | يقيس السكتات ويطلع مقاطع الكلام | مشترك |
| `02_captions.py` | توقيت كل كلمة على التايم-لاين الجديد | مشترك |
| `03_cut_zoom.py` | القص + زوم لكل مقطع + وسم bt709 | مشترك |
| `04_render_frames.js` | يرسم الفريمات (استئناف + نافذة) | الخفيف |
| `04b_remotion.sh` | يجهّز/يفتح/يرندر مشروع ريموشن | ريموشن |
| `05_sfx.py` | المؤثرات الصوتية من `sfx.json` | مشترك |
| `06_encode.sh` | يجمّع الفريمات + الصوت | الخفيف |
| `06b_master.sh` | ‎-14 LUFS + خلفية صوتية بخفض تلقائي | مشترك |
| `07_contact_sheet.sh` | ورقة لقطات وحدة (توفير توكنز) | مشترك |
| `08_safe_check.js` | المنطقة الآمنة + الهوك | الخفيف (وبريموشن: `"guides":true` تعطيك المناطق حيّة بالاستوديو) |
| `09_srt.py` | ملف ترجمة + نص الكابشن | مشترك |
| `10_script_edit.py` | شيل جملة من النص → تنشال من الفيديو | مشترك |
| `11_behind_text.js` + `personmask.swift` | أنماط القصّ الثلاثة (ورا الشخص · قدام اللوحة · راس برّا الكرت) | الخفيف |
| `13_collage.js` · `13_collage_sfx.py` · `collage.TEMPLATE.html` | أسلوب الكولاج: قصّ + مخطِّط + رسم + مؤثرات → `ad-collage.mp4` |
| `12_montage.py` | **وضع المونتاج**: يفحص مجلد مقاطع، يختار أحلى لحظة بكل واحد، ويركّبها | مستقل |
| `15_podcast.py` · `16_render_pod.js` · `compose.PODCAST.html` · `facetrack.swift` | **وضع البودكاست**: مزامنة كاميرات · عطب · من يتكلم · خطة مشاهد · رسم · تجميع | مستقل (ماك) |

---

## 🪟 ويندوز
`python` مو `python3` · المسارات بصيغة `C:/…` · «الكلام ورا الشخص» والبودكاست ماك فقط. التفاصيل: `references/windows.md`.
